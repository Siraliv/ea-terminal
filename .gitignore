# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (VITE_ vars are public-safe but keep .env.local out of git regardless)
.env
.env.local
.env.*.local

# Test / build artifacts
coverage

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# UI Kit reference files dropped at the project root for design lookup.
# These are not part of the app — they're the orb-jnl kit's source the
# user shared so Claude could port specific values. Kept in the working
# tree for offline reference but never committed. (Vite's own index.html
# at the project root is unrelated and stays tracked — the kit doesn't
# have one here.)
/Primitives.jsx
/Screens.jsx
/Sidebar.jsx
/colors_and_type.css
/styles.css
